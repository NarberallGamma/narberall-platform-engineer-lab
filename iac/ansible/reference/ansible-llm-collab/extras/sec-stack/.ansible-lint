# SOPS files hold encrypted blobs on very long lines; linting them is pointless
# (yaml[line-length] kept failing on secrets.sops.yml).
exclude_paths:
  - inventory/group_vars/sec_stack/secrets.sops.yml
