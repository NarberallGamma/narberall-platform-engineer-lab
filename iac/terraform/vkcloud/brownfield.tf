# Brownfield inventory code.
# Private estate: inventory then import then plan No changes. Apply is a separate decision.
# Catalog: module.catalog / local.* from ./variables/

locals {
  brownfield_note = "sanitized showcase; prevent_destroy on instance/volume/attach; short ignore on instance"
}
