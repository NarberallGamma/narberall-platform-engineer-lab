# llm-compose-kit

**Status:** scaffold placeholder (Work PC)

Docker Compose (or k8s) kit: LLM API + optional vector DB + reverse proxy pattern.  
Home PC may document local Ollama variants under `practice/home-lab/`, not by overwriting this kit blindly.
