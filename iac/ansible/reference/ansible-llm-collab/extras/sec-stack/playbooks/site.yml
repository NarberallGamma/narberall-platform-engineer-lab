---
- name: sec-stack stack (VictoriaMetrics + Grafana + exporters)
  hosts: sec_stack
  roles:
    - dns
    - docker
    - sec_stack

- name: Node exporter on infrastructure VMs
  hosts: teleport:teleport_db
  roles:
    - node_exporter
