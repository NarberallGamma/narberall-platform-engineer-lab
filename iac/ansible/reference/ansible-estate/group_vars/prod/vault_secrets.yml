---
# Vault Secrets Configuration for PROD Environment
# Секреты для загрузки в Vault кластер prod
# ВАЖНО: Измените значения секретов перед развертыванием!

vault_addr: "https://{{ vault_virtual_ip }}"
vault_mount_point: "secret"
vault_api_version: "v1"

# Список секретов для загрузки в Vault
vault_secrets:
  # ========== TREASURY GLOBAL VALUES ==========
  - name: "treasury-global-values"
    path: "treasury-global-values"
    description: "Глобальные значения для всех сервисов"
    data:
      CHAIN_ID: "1"  # PROD: Mainnet Ethereum
      ETH_FEE: "0.005"
      ETH_USDT_ADDRESS: ""  # PROD: Mainnet USDT contract address
      PROVIDER_URL: ""  # PROD: Alchemy/Infura Mainnet URL
      QUOTE_EXPIRATION_JOB_DELAY: "5m"
      RETRY_TX_DELAY: "5m"
      SMS_CODE_RETRY_DELAY_IN_SECONDS: "60"
      TRON_PROVIDER_URL: "https://api.trongrid.io/v1"  # PROD: Mainnet Tron
      TRON_USDT_ADDRESS: ""  # PROD: Mainnet Tron USDT address
      TRX_FEE: "20.0"
      TREASURY_BASE_URL: "https://operator.example.com"  # PROD domain
      TREASURY_COMPANY_CARD_PAGE_URL: "https://operator.example.com/companies/"
      TREASURY_CONTRACT_PAGE_URL: "https://operator.example.com/contracts/"
      TREASURY_FROM: "no-replay@example.com"
      TREASURY_HELP_EMAIL: "no-replay@example.com"
      TREASURY_HELP_PHONE: "88005553535"
      TREASURY_LOGIN_PRIMARY_PAGE_URL: "https://operator.example.com/login"
      locale: "RU"

  # ========== TREASURY KAFKA ==========
  - name: "treasury-kafka"
    path: "treasury-kafka"
    description: "Kafka и PostgreSQL credentials"
    data:
      akhqAdminPassword: ""  # PROD: bcrypt hash
      akhqReaderPassword: ""  # PROD: bcrypt hash
      kafkaClientPassword: ""  # PROD: Kafka client password
      kafkaInterBrokerPassword: ""  # PROD: Kafka inter-broker password
      postgresPassword: ""  # PROD: Strong PostgreSQL password
      postgresUser: "treasury_user"

  # ========== TREASURY AUTH PROVIDER APP ==========
  - name: "treasury-auth-provider-app"
    path: "treasury-auth-provider-app"
    description: "Auth Provider Service секреты"
    data:
      data: "general configuration data"
      private_key: ""  # PROD: RSA 2048 private key (base64)
      public_key: ""  # PROD: RSA 2048 public key (base64)
      spring.datasource.password: ""  # PROD: PostgreSQL password
      spring.datasource.username: "treasury_user"

  # ========== TREASURY AML APP ==========
  - name: "treasury-aml-app"
    path: "treasury-aml-app"
    description: "AML Service секреты"
    data:
      data: "general configuration data"
      kyt.api-key-id: ""  # PROD: Real KYT API key ID
      kyt.secret-key: ""  # PROD: Real KYT secret key
      kyt.shard.token: ""  # PROD: Real KYT shard token
      spring.datasource.password: ""  # PROD: PostgreSQL password
      spring.datasource.username: "treasury_user"

  # ========== TREASURY AUDIT APP ==========
  - name: "treasury-audit-app"
    path: "treasury-audit-app"
    description: "Audit Service секреты"
    data:
      data: "general configuration data"
      spring.datasource.password: ""  # PROD: PostgreSQL password
      spring.datasource.username: "treasury_user"

  # ========== TREASURY CONTRACT APP ==========
  - name: "treasury-contract-app"
    path: "treasury-contract-app"
    description: "Contract Service секреты"
    data:
      data: "general configuration data"
      spring.datasource.password: ""  # PROD: PostgreSQL password
      spring.datasource.username: "treasury_user"

  # ========== TREASURY CSP APP ==========
  - name: "treasury-csp-app"
    path: "treasury-csp-app"
    description: "CSP Service секреты"
    data:
      data: "general configuration data"
      private_key: ""  # PROD: RSA 2048 private key (base64)
      public_key: ""  # PROD: RSA 2048 public key (base64)
      rootCertificate: ""  # PROD: Root certificate (base64)
      spring.datasource.password: ""  # PROD: PostgreSQL password
      spring.datasource.username: "treasury_user"

  # ========== TREASURY KYT BITOK EMULATOR APP ==========
  - name: "treasury-kyt-emulator-app"
    path: "treasury-kyt-emulator-app"
    description: "KYT Bitok Emulator Service секреты"
    data:
      kyt.api-key-id: ""  # PROD: Same as treasury-aml-app
      kyt.secret-key: ""  # PROD: Same as treasury-aml-app
      kyt.shard.token: ""  # PROD: Same as treasury-aml-app

  # ========== TREASURY NOTIFICATION APP ==========
  - name: "treasury-notification-app"
    path: "treasury-notification-app"
    description: "Notification Service секреты"
    data:
      data: "general configuration data"
      sms-aero-service.password: ""  # PROD: Real SMS service password
      sms-aero-service.username: ""  # PROD: Real SMS service username
      spring.datasource.password: ""  # PROD: PostgreSQL password
      spring.datasource.username: "treasury_user"
      spring.mail.password: ""  # PROD: Real email password
      spring.mail.username: ""  # PROD: Real email username

  # ========== TREASURY ONBOARDING APP ==========
  - name: "treasury-onboarding-app"
    path: "treasury-onboarding-app"
    description: "Onboarding Service секреты"
    data:
      data: "general configuration data"
      spring.datasource.password: ""  # PROD: PostgreSQL password
      spring.datasource.username: "treasury_user"

  # ========== TREASURY OTP APP ==========
  - name: "treasury-otp-app"
    path: "treasury-otp-app"
    description: "OTP Service секреты"
    data:
      data: "general configuration data"
      spring.datasource.password: ""  # PROD: PostgreSQL password
      spring.datasource.username: "treasury_user"

  # ========== TREASURY REPORT APP ==========
  - name: "treasury-report-app"
    path: "treasury-report-app"
    description: "Report Service секреты"
    data:
      data: "general configuration data"
      file-storage.s3.accessKey: ""  # PROD: Real S3 access key
      file-storage.s3.secretKey: ""  # PROD: Real S3 secret key
      spring.datasource.password: ""  # PROD: PostgreSQL password
      spring.datasource.username: "treasury_user"

  # ========== TREASURY TX APP ==========
  - name: "treasury-tx-app"
    path: "treasury-tx-app"
    description: "Transaction Service секреты"
    data:
      data: "general configuration data"
      spring.datasource.password: ""  # PROD: PostgreSQL password
      spring.datasource.username: "treasury_user"

  # ========== TREASURY WEB APP ==========
  - name: "treasury-web-app"
    path: "treasury-web-app"
    description: "Web Service секреты"
    data:
      data: "general configuration data"
      file-storage.s3.accessKey: ""  # PROD: Real S3 access key
      file-storage.s3.secretKey: ""  # PROD: Real S3 secret key
      spring.datasource.password: ""  # PROD: PostgreSQL password
      spring.datasource.username: "treasury_user"

