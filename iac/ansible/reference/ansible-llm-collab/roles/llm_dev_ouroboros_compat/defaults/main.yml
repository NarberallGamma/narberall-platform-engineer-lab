---
# Ouroboros / OpenAI without role=system: patched official Qwen3.5-9B chat template.
# Enable: llm_dev_ouroboros_compat_enabled: true in group_vars/llm_dev/main.yml.
# Disable: false — template from the GGUF (strict Qwen: system must be first).
# Template source: roles/llm_dev_ouroboros_compat/files/qwen35_9b_ouroboros_patched_chat_template.jinja

llm_dev_ouroboros_compat_enabled: false

llm_dev_ouroboros_compat_dir: "{{ llm_dev_app_root }}/chat-templates"
llm_dev_ouroboros_compat_template_filename: "qwen35_9b_ouroboros_patched_chat_template.jinja"
llm_dev_ouroboros_compat_container_mount: "/chat-templates"
