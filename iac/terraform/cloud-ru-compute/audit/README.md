# Audit stack (read-only)

Same catalog maps as `../deploy/variables`. No `resource` blocks. Remote state key: `platform/audit/terraform.tfstate`.

Use this root to print VPC / CCE / RDS / ECS IDs and the `do_not_import` list without touching compute.
