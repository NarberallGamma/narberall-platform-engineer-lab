---
# Overlay for werf/helm. Copy to secret-values.yaml locally. Do not commit the live file.
ssh_key: CHANGE_ME
psql:
  app:
    host: pg.estate.example.com
    port: "5432"
    database: app
    user: backup
    password: CHANGE_ME
