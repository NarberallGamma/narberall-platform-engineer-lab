apiVersion: v2
name: policy-gateway
description: Estate policy gateway umbrella. One PVC (lookup + keep) and initContainersTemplate.
type: application
version: 0.1.0
appVersion: "2.13.0"

dependencies:
  - name: base-chart
    version: 0.2.1
    repository: "file://../_libs/base-chart"
    alias: application
    condition: application.enabled
