---
- name: Установить права на /etc/fstab
  ansible.builtin.file:
    path: /etc/fstab
    mode: '0644'

- name: Обновить права и владельца на критичных файлах
  ansible.builtin.file:
    path: "{{ item.path }}"
    owner: root
    group: root
    mode: "{{ item.mode }}"
  loop:
    - { path: '/etc/passwd',   mode: '0644' }
    - { path: '/etc/passwd-',  mode: '0600' }
    - { path: '/etc/group',    mode: '0644' }
    - { path: '/etc/group-',   mode: '0644' }
    - { path: '/etc/shadow',   mode: '0640' }
    - { path: '/etc/shadow-',  mode: '0640' }
    - { path: '/etc/gshadow',  mode: '0640' }
    - { path: '/etc/gshadow-', mode: '0640' }

- name: Права на sshd_config
  ansible.builtin.file:
    path: /etc/ssh/sshd_config
    owner: root
    group: root
    mode: '0600'

- name: Права на каталоги cron
  ansible.builtin.file:
    path: "{{ item }}"
    owner: root
    group: root
    mode: '0700'
    state: directory
  loop:
    - /etc/cron.d
    - /etc/cron.hourly
    - /etc/cron.daily
    - /etc/cron.weekly
    - /etc/cron.monthly
