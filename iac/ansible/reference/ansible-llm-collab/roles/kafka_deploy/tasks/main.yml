---
- name: Install (directories)
  ansible.builtin.import_tasks: install.yml
  tags: [kafka, kafka_install, kafka_deploy]

- name: Deploy compose
  ansible.builtin.import_tasks: deploy.yml
  tags: [kafka, kafka_install, kafka_manage, kafka_config, kafka_deploy]

- name: Restart by tag
  ansible.builtin.import_tasks: restart.yml
  tags: [never, kafka_restart, kafka_deploy]
