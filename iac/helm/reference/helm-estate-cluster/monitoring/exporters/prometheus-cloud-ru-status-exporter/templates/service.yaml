apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-cloud-ru-status-exporter.fullname" . }}
  namespace: {{ include "prometheus-cloud-ru-status-exporter.namespace" . }}
  labels:
    {{- include "prometheus-cloud-ru-status-exporter.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "prometheus-cloud-ru-status-exporter.selectorLabels" . | nindent 4 }}
