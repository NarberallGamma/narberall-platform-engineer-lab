---
- name: Install (directories)
  ansible.builtin.import_tasks: install.yml
  tags: [llm_dev, llm_dev_install, llm_dev_deploy]

- name: Deploy compose
  ansible.builtin.import_tasks: deploy.yml
  tags: [llm_dev, llm_dev_install, llm_dev_manage, llm_dev_config, llm_dev_deploy]

- name: Restart by tag
  ansible.builtin.import_tasks: restart.yml
  tags: [never, llm_dev_restart, llm_dev_deploy]
