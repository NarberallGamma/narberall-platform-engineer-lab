[Unit]
Description=A replication manager, and failover management tool for PostgreSQL
After=syslog.target
After=network.target

[Service]
Type=forking

User=postgres
Group=postgres

EnvironmentFile=/etc/default/repmgrd
PIDFile=/var/run/postgresql/repmgrd.pid

# Where to send early-startup messages from the server
# This is normally controlled by the global default set by systemd
# StandardOutput=syslog
ExecStart=
ExecStart=/usr/bin/repmgrd -f ${REPMGRD_CONF} -p /var/run/postgresql/repmgrd.pid ${REPMGRD_OPTS}
ExecStop=/usr/bin/kill -TERM ${MAINPID}
ExecReload=/usr/bin/kill -HUP ${MAINPID}
# Give a reasonable amount of time for the server to start up/shut down
TimeoutSec=300

[Install]
WantedBy=multi-user.target

