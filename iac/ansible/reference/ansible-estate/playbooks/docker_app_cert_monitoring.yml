---
# Deploy cert-monitoring (docker compose under /docker/apps/cert-monitoring)
# Hosts: inventory group [cert-monitoring] (any VM)
# Variables: group_vars/cert-monitoring.yml (vars_files — independent of the inventory path)

- name: Deploy cert-monitoring
  hosts: cert_monitoring
  become: true
  gather_facts: true

  vars_files:
    - "{{ playbook_dir }}/../group_vars/cert-monitoring.yml"

  pre_tasks:
    - name: Deployment info
      ansible.builtin.debug:
        msg:
          - "Host: {{ inventory_hostname }} ({{ ansible_host }})"
          - "App: {{ docker_app.slug }}"
          - "Path: {{ docker_app_base_dir }}/{{ docker_app.slug }}"
          - "Vault: {{ docker_app_vault_mount_point }}/{{ docker_app_vault_path }}"

  roles:
    - docker_app
