version: "3.9"

services:
  nifi:
    image: apache/nifi:2.6.0
    ports:
      - "8443:8443"
    environment:
        - SINGLE_USER_CREDENTIALS_USERNAME=admin
        - SINGLE_USER_CREDENTIALS_PASSWORD=CHANGE_ME
        - NIFI_WEB_PROXY_HOST=nifi.dev.example.com
