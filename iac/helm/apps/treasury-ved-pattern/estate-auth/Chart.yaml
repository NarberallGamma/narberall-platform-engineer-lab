apiVersion: v2
name: estate-auth
description: Thin umbrella for the estate auth service (OIDC issuer plus ExternalSecret)
type: application
version: 0.1.0
appVersion: "2.8.0"

dependencies:
  - name: base-chart
    version: 0.2.1
    repository: "file://../_libs/base-chart"
    alias: application
    condition: application.enabled
