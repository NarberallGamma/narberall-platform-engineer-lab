{{- if .Values.testconnection.enabled -}}
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "front-base.fullname" . }}-test-connection"
  labels:
    {{- include "front-base.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "front-base.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
{{- end }}
