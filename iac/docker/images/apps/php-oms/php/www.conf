[www]
user = www-data
group = www-data
listen = 127.0.0.1:9000
pm = static
pm.max_children = 200
pm.max_requests = 500
pm.status_path = /status
slowlog = /tmp/$pool.log.slow
request_slowlog_timeout = 10s
