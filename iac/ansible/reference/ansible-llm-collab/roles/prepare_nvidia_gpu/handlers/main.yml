---
- name: restart docker after nvidia runtime
  ansible.builtin.systemd:
    name: docker
    state: restarted
    daemon_reload: true

- name: reboot after nvidia driver install
  ansible.builtin.reboot:
    reboot_timeout: 600
  when: prepare_nvidia_reboot_after_driver | bool
