# Placeholder addresses. All VMs on one cloud project.
all:
  children:
    sec_stack:
      hosts:
        monitoring-01.example.com:
          ansible_host: 10.10.1.243
    teleport:
      hosts:
        teleport.example.com:
          ansible_host: teleport.example.com
    teleport_db:
      # Список ВМ (не teleport-регистраций!): на 10.10.4.37 живёт pgbouncer,
      # обслуживающий две базы; сам postgres — на 10.10.4.30 (из pgbouncer.ini).
      hosts:
        estate-prod.example.com: { ansible_host: 10.10.16.150 }
        platform-1c-psql.example.com: { ansible_host: 10.10.2.251 }
        pay-prod-pgbouncer.example.com: { ansible_host: 10.10.4.37 }
        pay-prod-psql.example.com: { ansible_host: 10.10.4.30 }
