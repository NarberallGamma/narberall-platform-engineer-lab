# Cloud.ru Status Exporter config (overridden via Helm/ConfigMap)
# platform — for service_availability (service statuses)
# emergency_platforms — for actual/planned events; if empty, platform is used. Completed events are not collected.
platform: advanced
api_base_url: "https://api.cloud.ru"
scrape_interval_seconds: 300
