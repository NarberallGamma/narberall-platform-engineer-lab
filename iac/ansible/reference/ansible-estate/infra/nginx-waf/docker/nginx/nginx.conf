user www-data;

worker_processes  4;

error_log  /var/log/nginx/error-aggregate.log warn;
error_log  /dev/stderr warn;
pid        /var/run/nginx.pid;

include /etc/nginx/modules-enabled/*.conf;

events {
    worker_connections  2048;
}

http {
    include       /etc/nginx/mime.types;
    default_type  application/octet-stream;

    log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
                      '$status $body_bytes_sent "$http_referer" '
                      '"$http_user_agent" "$http_x_forwarded_for"';
    log_format  wallarm  '$remote_addr - $remote_user [$time_local] "$request" '
                         '$status $body_bytes_sent "$http_referer" '
                         '"$http_user_agent" "$http_x_forwarded_for" "$http_x_bug_bounty" "$host" '
                         '"$wallarm_request_cpu_time" "$wallarm_request_mono_time" "$wallarm_serialized_size" '
                         '"$wallarm_is_input_valid" "$wallarm_attack_type" "$wallarm_attack_type_list" "$upstream_status" '
                         '"$uri" "$http_x_verdict" "$http_x_device_id" "$http_x_session_id"';

    # Shared access/error in files on the host mount (names other than access.log/error.log: the image may replace those with stdio symlinks).
    access_log  /var/log/nginx/access-aggregate.log wallarm;

    sendfile        on;
    #tcp_nopush     on;

    keepalive_timeout  65;

    server_names_hash_bucket_size 128;

    #gzip  on;

    real_ip_header X-Forwarded-For;
    real_ip_recursive on;
    set_real_ip_from 198.51.100.0/24;
    set_real_ip_from 198.51.100.0/24;
    set_real_ip_from 198.51.100.0/24;
    set_real_ip_from 198.51.100.0/24;
    set_real_ip_from 198.51.100.0/24;
    set_real_ip_from 198.51.100.0/24;    

    map $http_upgrade $connection_upgrade {
        default upgrade;
        ''      close;
    }

    # Conditional access_log wallarm-security.log is set in the vhost via if=$wallarm_attack_type_list (Wallarm module variable).
    # Do not use map → $log_wallarm_security: nginx 1.14.x from the WAF image fails nginx -t with unknown "log_wallarm_security" variable.

    include /etc/nginx/conf.d/*.conf;

    server_tokens off;
    proxy_buffers 16 16k;
    proxy_buffer_size 16k;

    ssl_protocols TLSv1.2 TLSv1.3;
}
