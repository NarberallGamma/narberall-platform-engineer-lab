apiVersion: v1
kind: Service
metadata:
  name: {{ include "prometheus-cloudeye-exporter.fullname" . }}
  namespace: {{ include "prometheus-cloudeye-exporter.namespace" . }}
  labels:
    {{- include "prometheus-cloudeye-exporter.labels" . | nindent 4 }}
  {{- with .Values.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: {{ .Values.service.port }}
    targetPort: http
    protocol: TCP
    name: http
  selector:
    {{- include "prometheus-cloudeye-exporter.selectorLabels" . | nindent 4 }}
