---
- name: 3X-UI in Docker (profile jump / proxy)
  hosts: xui_proxy
  gather_facts: true
  become: true

  vars:
    xui_artifacts_dir: "{{ playbook_dir }}/../artifacts/xui"

  roles:
    - role: xui_docker
