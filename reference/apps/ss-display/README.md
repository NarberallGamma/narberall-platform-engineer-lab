# ss-display (example)

SteelSeries Apex OLED on Linux: example YAML + systemd **user** unit with placeholder paths (`%h/src/ss-display`, `/path/to/your/animation.gif`). Copy to `config.local.yaml` / `~/.config/systemd/user/` locally.

Practice: [`../../../practice/home-lab/pet-projects.md`](../../../practice/home-lab/pet-projects.md).
