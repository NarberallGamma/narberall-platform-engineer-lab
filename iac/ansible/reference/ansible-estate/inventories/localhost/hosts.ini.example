# Local run (scripts/run/ with --local): playbook on the same host.
# connection=local, SSH is not used.

[local]
localhost ansible_connection=local

[local:vars]
ansible_user=root
become=true
become_method=sudo
