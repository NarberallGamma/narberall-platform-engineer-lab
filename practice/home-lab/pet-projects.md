# Pet projects index

Small repos that support Platform / Linux / AI positioning. Entertainment-only work is omitted.

| Project | One-liner | Why a lead cares |
|---------|-----------|------------------|
| SSH tunnel Android + Docker | Custom failover client for the edge platform | [android-ssh.md](android-ssh.md) — [`../../reference/apps/ssh-tunnel-android/`](../../reference/apps/ssh-tunnel-android/) |
| Ansible 3X-UI / Xray | Roles + runner image from zero | [edge-platform.md](edge-platform.md) — [`../../reference/ansible-edge/`](../../reference/ansible-edge/) |
| SD WebUI Docker + Kohya | GPU compose + LoRA presets | [ai-lab.md](ai-lab.md) — [`../../reference/ai/`](../../reference/ai/) |
| ss-display (fork) | SteelSeries OLED on Linux: venv, example config, systemd user unit | HID / userspace — [`../../reference/apps/ss-display/`](../../reference/apps/ss-display/) |
| Lian Li Linux stack | Daemon + OpenRGB + deploy/verify scripts for AIO + fans | systemd, USB, hardware as code (host JSON stays local) |

Linux HID/cooling work is the same skill as day-2 hosts: example configs, no secrets in git, autostart units with placeholders.
